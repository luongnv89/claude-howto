# AI 代码生成的整洁代码规则

这些规则用于指导代码生成，目标是产出可维护、专业级质量的代码。

## 有意义的命名
- 使用“意图可见”的名称，让人一眼理解它为什么存在
- 避免误导性命名和无意义区分（如 `data`、`info`、`manager`）
- 使用可读、可搜索、可发音的名称
- 类名：使用名词（如 `UserAccount`、`PaymentProcessor`）
- 方法名：使用动词（如 `calculateTotal`、`sendEmail`）
- 避免依赖记忆映射和编码式命名（如匈牙利命名、前缀缩写）

## 函数
- 函数应尽量短小（理想情况 < 20 行）
- 只做一件事——遵循单一职责原则（SRP）
- 每个函数只包含一个抽象层级
- 参数数量要克制：0-2 最佳，最多 3 个，避免布尔开关参数
- 避免副作用——函数应与其名称语义一致
- 命令（改变状态）与查询（返回信息）要分离
- 优先使用异常而非错误码

## 注释
- 优先写“自解释代码”，能不写注释就不写
- 好注释：法律信息、风险警告、TODO、公共 API 文档
- 坏注释：冗余、误导、为糟糕代码辩解
- 不要注释掉代码——直接删除（版本控制会保留历史）
- 如果你必须写注释，先考虑是否应该重构代码

## 格式化
- 文件应保持小而专注
- 纵向布局：相关概念靠近，空行分隔不同概念
- 横向布局：控制行宽（80-120 字符）
- 使用一致缩进和团队统一风格
- 将相关函数组织在一起

## 对象与数据结构
- 对象：将数据隐藏在抽象之后，通过方法暴露行为
- 数据结构：暴露数据，尽量少行为
- 迪米特法则：只与“直接朋友”通信，避免 `a.getB().getC().doSomething()`
- 不要无脑暴露 getter/setter 导致内部结构泄漏

## 错误处理
- 使用异常，而不是返回码或错误标记
- 对可能失败的代码，先写 `try-catch-finally`
- 异常信息要包含上下文
- 不返回 `null`——返回空集合或使用 Optional/Maybe
- 不要把 `null` 当参数传递

## 类
- 小类优先：衡量标准是职责数量，而不是行数
- 单一职责原则：一个类只有一个变更原因
- 高内聚：类内变量应被多个方法共同使用
- 低耦合：类之间依赖尽量少
- 开闭原则：对扩展开放，对修改关闭

## 单元测试
- 满足 F.I.R.S.T.：快速、独立、可重复、自验证、及时
- 每个测试一个断言目标（或一个概念）
- 测试代码质量应与生产代码同等重视
- 测试命名要可读，能说明验证意图
- 使用 Arrange-Act-Assert 模式

## 代码质量原则
- **DRY（Don't Repeat Yourself）**：杜绝重复
- **YAGNI（You Aren't Gonna Need It）**：不要为假想需求提前设计
- **KISS（Keep It Simple）**：避免不必要的复杂性
- **童子军法则（Boy Scout Rule）**：离开时让代码比接手时更干净

## 应避免的代码异味
- 过长函数或过大类
- 重复代码
- 死代码（未使用变量、函数、参数）
- Feature Envy（方法过度依赖其他类）
- Inappropriate Intimacy（类之间知道彼此太多内部细节）
- 过长参数列表
- Primitive Obsession（过度使用原始类型而非小对象建模）
- 大量 switch/case（可考虑多态）
- 临时字段（仅在少数场景使用的类成员）

## 并发
- 并发代码与普通业务代码分离
- 限制同步/锁定数据的作用域
- 使用线程安全集合
- 保持同步区尽可能小
- 清楚理解执行模型与并发原语

## 系统设计
- 将“构建”与“使用”分离（依赖注入）
- 复杂对象创建优先使用工厂或构建器
- 面向接口编程，而非面向具体实现
- 组合优于继承
- 在能简化问题时再用设计模式，而不是炫技

## 重构
- 持续小步重构，而不是一次性大改
- 重构前后都要有通过的测试
- 小步前进：一次只做一个改动
- 常见重构：提取方法、重命名、移动、内联

## 文档
- 自文档化代码 > 注释 > 外部文档
- 公共 API 必须有清晰文档
- 文档中应包含示例
- 文档尽量贴近代码（理想情况是与代码共置）

---

**核心理念**：代码被阅读的次数约为编写的 10 倍。优化目标应是可读性与可维护性，而不是“聪明技巧”。
