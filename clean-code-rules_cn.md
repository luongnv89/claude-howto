# AI 代码生成的整洁代码规则

这些规则指导代码生成，以产出可维护的、专业级别的代码。

## 有意义的命名
- 使用能揭示意图的名称，说明某事物存在的原因
- 避免误导性和无意义的区分（如 `data`、`info`、`manager`）
- 使用可发音、可搜索的名称
- 类名：名词（如 `UserAccount`、`PaymentProcessor`）
- 方法名：动词（如 `calculateTotal`、`sendEmail`）
- 避免思维映射和编码（匈牙利命名法、前缀）

## 函数
- 保持函数小巧（理想情况下 < 20 行）
- 只做一件事 —— 单一职责原则
- 每个函数保持一个抽象层级
- 限制参数数量：理想 0-2 个，最多 3 个，避免标志参数
- 无副作用 —— 函数应该做其名称所说的事
- 将命令（改变状态）与查询（返回信息）分离
- 优先使用异常而非错误码

## 注释
- 代码应自解释 —— 尽可能避免注释
- 好的注释：法律信息、警告、TODO、公共 API 文档
- 坏的注释：冗余的、误导性的或用于解释糟糕代码的
- 永远不要注释掉代码 —— 直接删除（版本控制保存历史）
- 如果需要注释，考虑重构代码

## 格式化
- 保持文件小巧且专注
- 垂直格式：相关概念靠近，空行分隔概念
- 水平格式：限制行长度（80-120 个字符）
- 使用一致的缩进和团队风格
- 将相关函数分组

## 对象与数据结构
- 对象：通过抽象隐藏数据，通过方法暴露行为
- 数据结构：暴露数据，具有最少的行为
- 迪米特法则：只与直接朋友交谈，避免 `a.getB().getC().doSomething()`
- 不要盲目通过 getter/setter 暴露内部结构

## 错误处理
- 使用异常，而非返回码或错误标志
- 当代码可能失败时，首先编写 `try-catch-finally`
- 在异常消息中提供上下文
- 不要返回 `null` —— 返回空集合或使用 Optional/Maybe
- 不要将 `null` 作为参数传递

## 类
- 小类：按职责衡量，而非行数
- 单一职责原则：只有一个改变的原因
- 高内聚：类变量被许多方法使用
- 低耦合：类之间的依赖最少
- 开闭原则：对扩展开放，对修改关闭

## 单元测试
- 快速、独立、可重复、自验证、及时（F.I.R.S.T.）
- 每个测试一个断言（或一个概念）
- 测试代码质量等同于生产代码质量
- 可读的测试名称，描述正在测试的内容
- 安排-执行-断言（Arrange-Act-Assert）模式

## 代码质量原则
- **DRY（不要重复自己）**：无重复
- **YAGNI（你不会需要它）**：不要为假设的未来构建
- **KISS（保持简单）**：避免不必要的复杂性
- **童子军规则**：让代码比你发现时更整洁

## 需要避免的代码坏味道
- 过长的函数或类
- 重复代码
- 死代码（未使用的变量、函数、参数）
- 依恋情结（方法对其他类更感兴趣）
- 不恰当的亲密（类之间了解太多关于彼此）
- 过长的参数列表
- 基本类型偏执（过度使用基本类型而非小对象）
- Switch/case 语句（考虑多态）
- 临时字段（只有时候才使用的类变量）

## 并发
- 将并发代码与其他代码分开
- 限制同步/加锁数据的范围
- 使用线程安全的集合
- 保持同步区域小巧
- 了解你的执行模型和原语

## 系统设计
- 将构建与使用分离（依赖注入）
- 使用工厂、构建器进行复杂对象创建
- 面向接口编程，而非实现
- 优先使用组合而非继承
- 在简化时应用设计模式，而非炫耀

## 重构
- 持续重构，而非大批量重构
- 重构前后始终有通过的测试
- 小步骤：每次一个改变
- 常见重构：提取方法、重命名、移动、内联

## 文档
- 自文档化代码 > 注释 > 外部文档
- 公共 API 需要清晰的文档
- 在文档中包含示例
- 将文档保持在代码附近（最好在代码中）

---

**核心理念**：代码被阅读的次数是编写次数的 10 倍。为可读性和可维护性优化，而非技巧性。
